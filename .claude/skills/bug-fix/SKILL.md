---
name: allcll-bug-fix
description: 버그 수정·검증 로직 추가·작은 기능 보완 등 동작을 변경하는 작업 시 자동 발동. 리팩토링과 다르게 의도적인 동작 변경이 목표. 변경 영향과 검증 단계 의무화.
---

# 올클 버그 수정 가이드 (v1 - baseline 기반)

이 skill은 **fix 영역 baseline 관찰**(2026-04-25)을 근거로 작성됨.
관찰된 부족함을 원칙으로 기록해 같은 실수 방지.

## 작업 범위 정의

### 적용 대상

- 보고된 버그 수정
- 검증 로직 추가 (validation, 권한 등)
- 응답 형식 보완
- 작은 기능 추가 (단일 엔드포인트 수준)

### 적용 안 함

- 큰 기능 추가 (여러 컴포넌트 신설) — 별도 설계 단계 필요
- 도메인 모델 변경 — 영향 범위 너무 넓음
- 동작 보존 작업 — refactoring skill 사용

## 작업 순서 (의무)

### Step 1: 버그 재현 또는 시나리오 명확화

수정 시작 전:

1. 기존 코드의 **현재 동작** 파악
2. **기대 동작** 명시
3. 둘의 차이 = 수정해야 할 것

가능하면 **재현 테스트 먼저 작성**:

- 빨간색 (실패) 테스트 = 버그의 정의
- 수정 후 초록색 (통과) = 버그 해결 증거

baseline 관찰 (2026-04-25): 이 단계 통째로 빠뜨림.
"테스트는 변경 후에도 안 짬" → 회귀 방지 없음.

### Step 2: 기존 패턴 탐색

새로 만들지 말고 **기존 패턴 답습**:

- 비슷한 작업이 프로젝트에 있는지 Grep
- 적용 라이브러리·프레임워크 확인
- 컨벤션 발견하면 그대로 따름

baseline 관찰: 이 단계는 잘 됨 (AdminNoticeApi 패턴 발견). 강화 유지.

### Step 3: 영향 범위 점검

수정 위치 외에 같은 문제가 있을 수 있는 곳:

- 같은 도메인의 다른 API
- 같은 패턴의 다른 클래스
- 호출자 (이 코드를 쓰는 곳)

발견 시:

- 함께 수정 가능 → 범위 명시 후 수정
- 별도 작업 가치 → 사용자에게 보고

### Step 4: 구현

기존 패턴 따라 최소 변경:

- 새 의존성 추가 최소화
- 기존 컨벤션 유지
- 변경 라인 수 작게

### Step 5: 검증 (의무)

❌ **컴파일만으로 작업 종료 금지**

다음을 모두 수행:

1. **컴파일**: `./gradlew compileJava -q`
2. **관련 테스트**: `./gradlew :test --tests "*관련패턴*"`
3. **재현 테스트 추가** (Step 1에서 못했으면 여기서):
    - 버그 케이스 → 통과 확인
    - 정상 케이스 → 통과 확인
4. **전체 테스트** (영향 범위 큰 변경의 경우): `./gradlew :test`

baseline 관찰 (2026-04-25): 컴파일만 함. 테스트 단계 누락.
**이게 가장 큰 약점**이라 의무화 강조.

## 작업 범위 분리 원칙

작업 중 발견한 인접 문제는 **명시적으로 분리**:

> "원래 보고 받은 X는 수정했습니다.
> 작업 중 Y 문제도 발견했는데, 이건 [이유]로 별도 판단이 필요해
> 이번엔 손대지 않았습니다. 추가 작업 원하시면 알려주세요."

baseline 관찰: 이미 잘 됨 (HttpMessageNotReadable 처리 분리 보고).
강화 유지.

## 자가 보고 형식

작업 끝나면 다음 포함:

1. **변경 파일 목록 + 라인 수**
2. **변경 핵심 한 줄 요약**
3. **검증 단계 결과** (컴파일 / 테스트 / 추가 작업)
4. **분리한 인접 문제** (있으면)
5. **다음 단계 제안** (있으면)

## 절대 원칙

- 컴파일만 통과시키고 작업 완료 선언 금지
- 기존 테스트가 깨지면 무시하지 말 것 (분석 → 정당화 또는 롤백)
- 버그 재현 테스트 없이 "고쳤다"고 하지 말 것
- 사용자가 명시한 범위 외 임의 확장 금지
- 응답 형식·API 명세 변경 시 사용자 확인 필수

## 근거

fix 영역 baseline 관찰 (2026-04-25):

잘한 부분 (skill로 강화):

- 기존 패턴 답습 (AdminNoticeApi 같은 참고 사례 발견)
- 최소 변경
- 한계 명시 (별도 판단 분리)

부족한 부분 (skill로 보강):

- 버그 재현 테스트 안 짬
- 컴파일만 하고 테스트 실행 안 함
- 영향 범위 점검 제한적

→ 부족함을 의무 원칙으로 명시.

## 다음 검증

이 skill 적용 후 같은 종류 작업(검증 로직 추가, 작은 버그 수정)을 시켜
baseline 대비 차이 측정.

특히 확인할 것:

- 테스트 자발적 작성 여부
- 검증 단계 자발적 수행 여부
- 영향 범위 점검 깊이
